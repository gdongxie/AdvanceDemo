<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zzl.androidadvancedemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label=""
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".chapter2.MaterialDesignActivity" />
        <activity android:name=".chapter2.SnackBarActivity" />
        <activity android:name=".SplashActivity" />
        <activity android:name=".chapter3.ViewActivity" />
        <activity android:name=".chapter3.CustomActivity" />
        <activity android:name=".chapter3.CombinationActivity" />
        <activity android:name=".chapter3.CustomViewGroupActivity"></activity>
    </application>

</manifest>